# LawTracker — Agent Instructions

At the start of any session that touches this project:

1. Read `ROADMAP.md` — it holds the authoritative backlog, the "Last session summary" block for cold pickup, and the session-discipline rules under "For future agents".
2. Read `COMPLETED.md` only when your work overlaps a closed item's territory; otherwise skip it.
3. Invoke the `karpathy-guidelines` skill via the Skill tool before writing or editing code.

Project layout:

- `src/lawtracker/` — package source (src/ layout).
- `tests/` — pytest.
- `design/` — short per-feature design notes (see `design/README.md`).
- `pyproject.toml` — project metadata, runtime deps, dev tools (pytest, ruff, mypy).

Do not duplicate session-discipline guidance here — keep it single-sourced in `ROADMAP.md` under "For future agents".
